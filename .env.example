# Base URL the Next.js server uses to reach the backend inside the Docker
# network. "backend" is the docker-compose service name; see
# pinch/docker-compose.yml. Not prefixed with NEXT_PUBLIC_ since it's only
# ever read server-side.
BACKEND_INTERNAL_URL=http://backend:8000
